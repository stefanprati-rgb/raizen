# =============================================================================
# CONFIGURAÇÃO DO EXTRATOR DE CONTRATOS RAÍZEN
# =============================================================================
# Copie este arquivo para .env e preencha com suas credenciais
# Nunca commite o arquivo .env no Git!

# -----------------------------------------------------------------------------
# GEMINI AI (Obrigatório para mapeamento automático)
# -----------------------------------------------------------------------------
# Obtenha sua chave em: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=AIzaSyC0SOZ9tSYLRt14N61kK9R-dxijfcMebUI

# Modelo a usar (padrão: gemini-2.0-flash)
# GEMINI_MODEL=gemini-2.0-flash

# -----------------------------------------------------------------------------
# CONFIGURAÇÕES DE PROCESSAMENTO (Opcional)
# -----------------------------------------------------------------------------
# Número de workers para processamento paralelo (padrão: CPU - 1)
# MAX_WORKERS=8

# Timeout para OCR em segundos (padrão: 30)
# OCR_TIMEOUT=30

# -----------------------------------------------------------------------------
# LOGGING (Opcional)
# -----------------------------------------------------------------------------
# Nível de log: DEBUG, INFO, WARNING, ERROR (padrão: INFO)
# LOG_LEVEL=INFO

# Diretório de logs (padrão: output/logs)
# LOG_DIR=output/logs
